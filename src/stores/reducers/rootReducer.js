const initState = {
  users: [
    { id: 1, name: "Long" },
    {
      id: 2,
      name: "Link",
    },
  ],
};
const rootReducer = (state, action) => {
  return state;
};
export default rootReducer;
